const $canvas = $("#pacman-canvas");
const ctx = $canvas.getContext("2D");

function initMap(){
    // For each row
    for(let i = 0; i < g_pacman_grid.height; i++){
        // For each column
        for(let j = 0; j < g_pacman_grid.width; j++){
            
        }
    }
}

function updateMap(arg_updateMap){}

function animateAgent(arg_agent){}


